apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: staging
resources:
  - ../../base
patches:
  - path: patch-replicas.yaml
configMapGenerator:
  - name: demo-app-config
    behavior: merge
    literals:
      - environment=staging